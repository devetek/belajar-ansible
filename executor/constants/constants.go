package constants

const ENV_VARS_COMMON = "ENV"

var (
	ANSIBLE_USER_KNOWN_HOST = "~/.ssh/known_hosts"
	ANSIBLE_TMP_KEY         = "./tmp-key"
	ANSIBLE_TMP_INVENTORY   = "inventory/temporary-inventory.json"
)
